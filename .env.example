# Frontend Environment Variables (Vite)
# When developing locally, it defaults to http://localhost:8787/api/chat
# When deploying to GitHub Pages, set this to your deployed Cloudflare Worker endpoint:
# VITE_API_CHAT_URL=https://portfolio-gemini-worker.<your-subdomain>.workers.dev/api/chat
